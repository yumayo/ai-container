# テスト用マウント設定
./:/test

# 環境変数を使った例（通常の展開）
$HOME/.bashrc:/test/test-bashrc
${PWD}:/test/test-env-mount

# エラーを発生させる例（未定義の環境変数）
# 以下のコメントを外すと、環境変数が未定義の場合にエラーが発生します
# ${UNDEFINED_VAR?}:/test/should-fail
# ${ANOTHER_UNDEFINED:?Variable ANOTHER_UNDEFINED is required}:/test/should-fail-with-message

# デフォルト値を使う例
${OPTIONAL_VAR:-./}:/test/optional
